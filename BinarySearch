#include<stdio.h>
int bs(int a[],int n,int key){
	int s=0,e=n-1,m=(s+e)/2,loc;
	while(s<=e){
		
		if(key==a[m]){
			loc=m;
			return loc+1;
		}
		else if(key<a[m]){
			e=m-1;
		}
		else
			s=m+1;
		m=(s+e)/2;
	}
	return -1;
}
int main(){
	int n;
	scanf("%d",&n);
	int a[n];
	for(int i=0;i<n;i++){
		scanf("%d",&a[i]);
	}
	int key,loc=0,c;
	scanf("%d",&key);
	return 0;
}
	
	
